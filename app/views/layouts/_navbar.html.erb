<nav id="navigation" class="navbar navbar-default" role="navigation">
  <div class="container-fluid" >
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle float-right" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div id="logo" class="float-left">
      <img src="/images/nextbridge.png">
    </div>
    <div id="company-name" class="float-left" style="">
      <h3>Kantoor</h3>
    </div>
    <div id="nav-top-links" class="float-right">
      <ul>
        <%if !session[:username].blank? and !session[:current_resource_id].nil?%>
              <li><%=link_to session[:username],resource_path(:id => session[:current_resource_id])%> |</li>
        <% elsif user_signed_in? %>
              <li><%=link_to current_user.login_id, resource_path(current_user.userable_id)%></li>
        <%end%>
        <li><a href="#">Configuration |</a></li>
        <%if !session[:username].blank?%>
           <li><%=link_to :Logout ,login_signout_path%></li>
        <% elsif user_signed_in? %>
             <li><%= link_to('Logout', destroy_user_session_path, :method=>'delete') %> </li>
        <%else%>
            <li><%=link_to :Login ,login_new_path%></li>
        <%end%>
      </ul>
    </div>
    <div class="clear-both"></div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div  class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a class="active" href="#">Dash Board</a></li>
        <li><%=link_to :Leads,leads_path%></li>
        <li><%=link_to :Projects,projects_path%></li>
        <li><%=link_to :Resources,resources_path%></li>
        <li><%=link_to :Clients,clients_path%></li>
        <li><a href="#">Directory</a></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
